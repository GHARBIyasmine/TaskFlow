export interface LoginResponseI {
    refresh: string;
    access: string;
    
  }

  export interface RegisterResponseI {
    status: number;
    message: string;
  }
export interface reAthenticatePopupInfo {
  message : string
}

